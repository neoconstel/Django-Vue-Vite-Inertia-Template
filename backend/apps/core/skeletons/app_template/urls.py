from django.urls import path

from . import views

# set app_name here for name-spacing the urls (e.g app_name = 'home')
# app_name = 

'''
urlpatterns = [
    path("", views.function_view, name="index"),
    path('endpoint/', views.ClassView.as_view(), name='example_list'),
]
'''

